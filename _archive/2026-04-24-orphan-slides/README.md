# Archived orphan slide files — 2026-04-24

These two files were left over from an earlier numbering scheme of the
qp2-seminar deck and are NOT imported by `manifest.js`. They were
verified to have zero imports anywhere in `src/` before being archived
here.

| File | Replaces / superseded by | Notes |
| --- | --- | --- |
| `11-case-challenge.jsx` | `slides/07-case-challenge.jsx` | Earlier "Slide 06" version of the CS1 challenge spine layout. Same `ChallengeStack` + `SpineDiamond` (outside) pattern. |
| `11b-case-strategy.jsx` | `slides/08-case-strategy.jsx` | Earlier version of the CS1 strategy three-decisions slide. Pre-`SlideGrid`, uses `SlideFrame` directly. |

Kept for archaeology — older patterns may be useful as references when
designing future slides. Do not re-import; the live versions diverged
significantly during the production sweep.
