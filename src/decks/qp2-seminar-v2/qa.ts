/**
 * qp2-seminar-v2 — anticipated / rehearsed Q&A per slide.
 *
 * Mirrors qp2-seminar/qa.js. See that file for the full convention
 * (per-slide markdown, ## Q: pattern for the count badge, override
 * semantics through useAnticipatedQA).
 *
 * Empty string = pane auto-collapses to a "Q&A · empty" header.
 */

const qa = {
  // Add entries as you rehearse, keyed by slide id.
};

export default qa;
