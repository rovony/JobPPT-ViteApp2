import React from 'react';
import QuoteCard from '@/components/deck/patterns/QuoteCard';

export default function Slide02() {
  return (
    <QuoteCard
      quote="We weren't drowning in data. We were drowning in decisions."
      attribution="Every operator we spoke to"
      role="before we started"
    />
  );
}