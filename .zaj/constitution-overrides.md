# JobPPT-App2 — Constitution Overrides

> Project-specific overrides to the `zaj-vibecode` `_CONSTITUTION.md`.
> Tier-1 hard rules cannot be overridden. Tier-2 soft preferences can.

## Active overrides

_(none currently — base constitution applies as-is)_

## Notes for future overrides

If you need to override a Tier-2 rule, add it here with:
- The rule being overridden (link to `_CONSTITUTION.md` section)
- The override (what's different in this project)
- The reason (why this project diverges)
- Date added

Example template:

```
### Override: Card border weight

Base rule: cards use 1px hairline border (zaj-design Editorial-Motion default).

This project: HeroTile uses 1.5px solid case-color border + 4px left accent rail.

Reason: clinical-pharm decks need rapid scan parity between case cards.
The deck shipped with this pattern in v1/v2 already; consistency wins
over base-rule conformance.

Date added: 2026-05-06
```
